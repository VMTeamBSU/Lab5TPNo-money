language: c
os: linux

script: 
      - mkdir build
      - cd build
      - cmake .. -DCMAKE_BUILD_TYPE=Debug -G "Unix Makefiles"
      - make all 
      - ./tst/ExampleProject_tst

deploy:
  provider: s3
  access_key_id: "AKIAJZ4M5PSXYYOTSMOQ"
  secret_access_key: "mg+3dzsrDkJ1jFh+QT7k/3ft0N9wnzmkOQzoRdc4"
  bucket: "S3 Bucket

